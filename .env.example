# Copy this file to .env and adjust the values for your setup.
# .env is git-ignored and never committed - keep endpoint/infra details local.

# Base URL of your Ollama server's chat endpoint
OLLAMA_URL="http://localhost:11434/api/chat"

# Any Ollama model that supports structured JSON output (format schema)
OLLAMA_MODEL="llama3.1:8b"

# Context window size for the Ollama requests. Optional - defaults to 8192 if
# unset. Must be big enough for the whole prompt (instructions + fetched lyrics
# + recent-quote list); Ollama silently truncates anything longer, and server
# defaults are typically much smaller than what the model itself supports.
OLLAMA_NUM_CTX="8192"

# Comma-separated list of targets to post to (currently just: mastodon)
POST_TARGETS="mastodon"

# MASTODON_URL: your instance's base URL
# MASTODON_ACCESS_TOKEN: Settings > Development > New Application
#   (needs at least the write:statuses scope)
MASTODON_URL="https://mastodon.social"
MASTODON_ACCESS_TOKEN=""

# Used by update_bands.sh: comma-separated Wikidata QIDs of the genres a band
# must have (or a subclass thereof) to be included in bands.txt. Look up a
# genre's QID by searching for it on https://www.wikidata.org and reading the
# "Q..." out of the URL. Default: rock, heavy metal, punk rock, hard rock.
WIKIDATA_GENRE_QIDS="Q11399,Q38848,Q3071,Q83270"
